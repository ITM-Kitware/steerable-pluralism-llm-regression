# @package _global_
defaults:
  - override /adm: reward_model

datasets: 
  - mic

alignment_target: mic/target_high

adm:
  instance:
    model_name: Ray2333/GRM-Llama3.2-3B-rewardmodel-ft

hydra:
  run:
    dir: outputs/mic_deterministic/reward_model/