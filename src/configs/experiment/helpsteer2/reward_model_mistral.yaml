# @package _global_
defaults:
  - override /adm: reward_model

datasets: 
  - helpsteer2

alignment_target: helpsteer2/target_high

adm:
  instance:
    model_name: weqweasdas/RM-Mistral-7B

hydra:
  run:
    dir: outputs/helpsteer2_deterministic_mistral/reward_model/